# Verificare

- sintaxă JavaScript și service worker;
- referințe locale și manifest PWA;
- navigare cu tastatura și focus vizibil;
- zoom 200%, reduced motion, high contrast și mod relaxat;
- export/import/reset, offline, responsive 320–1440px.

Rezultatele Lighthouse trebuie publicate numai după măsurare, nu estimate.

## English

Checks cover JavaScript syntax, the service worker, local references, the PWA manifest, keyboard navigation, visible focus, 200% zoom, reduced motion, high contrast, relaxed mode, export/import/reset, offline use and responsive layouts from 320px to 1440px. Lighthouse results are published only after measurement.
