# Security

Do not report private user data in public issues. The app stores progress locally and uses no backend. Report import-validation or script-injection concerns through a private GitHub security advisory when available.
